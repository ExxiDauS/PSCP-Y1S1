"""TheFunctionWithin"""


def main():
    first = float(input())
    second = float(input())
    third = float(input())
    forth = float(input())
    ffunctionaws = 2 * first
    gfunctionaws = 3 * (first**4) - (first**3) + 2 * (first**2) + 10
    hfunctionaws = ((third + first) ** 2) - (first * second) + (second**2)
    ifunctionaws = (
        (first**2)
        + (second**2)
        - (third**2) / (forth**2)
        - (2 * (first * forth))
        + (2 * first)
    )
    print(ffunctionaws * (ffunctionaws))
    print(gfunctionaws * ())


main()
